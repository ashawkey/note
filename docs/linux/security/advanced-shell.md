# advanced shell

```bash
(cmd) # run cmd in a sub-shell, so variable assignments won't influence current shell.

[ # an actual binary in /usr/bin/[, which is an alias for `test`, and will ignore if the last argument is not ]
```

